= render :partial => '/admin/global/header'
%h1.title New role
.form
  - form_for([:admin, @role]) do |f|
    = f.error_messages
    %p
      = f.label :name
      %br/
      = f.text_field :name
    %p
      = f.submit 'Create'
